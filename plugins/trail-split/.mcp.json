{
  "mcpServers": {
    "trail-split": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.js"],
      "env": {
        "PLUGIN_SUITE_BILLING_URL": "${PLUGIN_SUITE_BILLING_URL}",
        "TRAIL_SPLIT_LICENSE_KEY": "${TRAIL_SPLIT_LICENSE_KEY}"
      }
    }
  }
}
